package com.sujata.demo;
/*
 * interface : 100% abstract class
 * all the "methods" are by default "public abstract"
 * and all the "variables" are by default "public static final"
 * 
 * we create interface to communicate design decisions to our team
 * 
 * Syntax : interface <InterfaceName> { .... }
 * Naming convention for interface is : PascalCase
 * 
 * 1. from one or more interface we can create another interface too
 */

public interface Shape {

// by default : the method is public abstract
	void area();
//	public void area();
//	public abstract void area();
//	abstract void area();
}
